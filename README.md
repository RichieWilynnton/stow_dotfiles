1. install zsh, git, curl, stow, vim, fzf, tar, unzip, tar, gzip, ripgrep, npm
2. install cpp tools
2. install oh-my-zsh and setup zsh-nvm (by cloning the repo, https://github.com/lukechilds/zsh-nvm)
3. install nvim 0.12>= (download the tar or appimage yourself)
4. run MasonToolsInstall
